import './index.css';

// components
import Post from '../Post';

const PostsRow = ({ posts }) => {
  console.log(posts);

  return (
    <div className="profilerow-container">

    </div>
  )
}

export default PostsRow;